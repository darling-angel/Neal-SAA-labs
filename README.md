Explanation:
Updates the system and installs Apache (httpd).
Starts and enables the httpd service.
Fetches the EC2 instance ID from AWS metadata.
Creates an HTML file displaying the instance ID on a web page.
